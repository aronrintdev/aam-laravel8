version: '3'
networks:
  test:
    external: true
  db:
    external: true
  zto:
    driver: bridge

services:
  aam-webapp:
    networks:
      - 'test'
      - 'db'
      - 'zto'
    volumes:
      - .:/app
      - ./config/container-nginx.vhost.conf:/etc/nginx/conf.d/container-vhost.conf
      - ../../shared/storage:/app/storage
