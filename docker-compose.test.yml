version: '3'
networks:
  web:
    external: true
  db:
    external: true
  zto:
    driver: bridge

services:
  aam-webapp:
    networks:
      - 'web'
      - 'db'
      - 'zto'
    volumes:
      - .:/app
      - ./config/container-nginx.vhost.conf:/etc/nginx/conf.d/container-vhost.conf
      - ../../shared/storage:/app/storage
